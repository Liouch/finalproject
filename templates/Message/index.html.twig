{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
Message controller
{{dump(user)}}
{{dump(userMessages)}}
<div class="row p-5 justify-content-center">
    <div class="col-6">
        {% for messages in userMessages %}
            <div class="card mt-2 ">
                <div class="card-header" id="title_${this.id}">{{messages.iduser.name}}</div>
                <div class="card-body">
                    <p class="card-text">{{messages.message}}</p>
                </div>
            </div>

        {% endfor %}
               
        
        
    </div>
</div>
{% endblock %}